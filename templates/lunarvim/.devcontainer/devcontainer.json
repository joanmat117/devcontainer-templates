{
    "name": "LunarVim Development Environment",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "."
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-vscode.cpptools",
                "rust-lang.rust-analyzer"
            ],
            "settings": {
                "terminal.integrated.defaultProfile.linux": "bash",
                "terminal.integrated.fontFamily": "FiraCode Nerd Font"
            }
        }
    },
    "remoteUser": "lunaruser",
    "mounts": [
        "source=${localEnv:HOME}${localEnv:USERPROFILE}/.gitconfig,target=/home/lunaruser/.gitconfig,type=bind,consistency=cached"
    ],
    "postCreateCommand": "echo '✅ LunarVim installed. Use: lvim'",
    "postAttachCommand": "echo '🚀 Environment ready - LunarVim: lvim, Neovim: nvim'"
}
